package evacuation_simulation;

import jade.core.AID;
import repast.simphony.random.RandomHelper;

public class Independent extends Person{

	public Independent(AID resultsCollector){
		super(resultsCollector);
		
		independent = RandomHelper.nextIntFromTo(MIN_SCALE, MAX_SCALE/2);
		areaKnowledgeable = RandomHelper.nextIntFromTo(MIN_SCALE, MAX_SCALE/2);
	}
	
	
}
