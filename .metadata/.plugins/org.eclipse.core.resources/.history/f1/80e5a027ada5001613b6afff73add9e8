package evacuation_simulation;

import jade.core.AID;
import repast.simphony.random.RandomHelper;

public class IndependentKnowledgeableAltruistic extends Person{

	public IndependentKnowledgeableAltruistic(AID resultsCollector){
		super(resultsCollector);
				
		independent = RandomHelper.nextIntFromTo(MAX_SCALE / 2, MAX_SCALE);
		areaKnowledgeable = RandomHelper.nextIntFromTo(MAX_SCALE / 2, MAX_SCALE);
		altruistic = RandomHelper.nextIntFromTo(MIN_SCALE, MAX_SCALE/2);
	}
	
	
}
